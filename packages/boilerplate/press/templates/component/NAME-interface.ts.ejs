/*
* Your component must have a TypeScript interface. Use this file to create it
* Don't forget to add a brief JSDoc comment above all props. 
* Make sure to include all useful information inside your comment and to use JSDoc tags where necessary (deprecation, throwables, etc.)
*/
import { StyleProp, ViewStyle } from "react-native";
import { <%= props.pascalCaseName %>PresetNames } from "./<%= props.kebabCaseName %>-presets";

export interface <%= props.pascalCaseName %>Props {
  /**
   * An optional style override useful for padding & margin.
   */
  style?: ViewStyle;
  /**
   * Which preset is being used
   */
  preset?: <%= props.pascalCaseName %>PresetNames
  <%= props.componentProps.map((prop,i)=>{return(
  '/**\n' + 
  '   * JSDoc Description\n' +
  '   */\n'  +
  '  '+prop.prop + ': ' + prop.type)}) %>
}
